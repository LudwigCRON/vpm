name: "resync"
version: "0.0.2"
# description of the package
description: "package with resynchronization module"
# list of files for the design
designs:
  - edge_resync.v
# list of files for the constraints
constraints:
